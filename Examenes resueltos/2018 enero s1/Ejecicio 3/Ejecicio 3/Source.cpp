#include <iostream>
#include <vector>

using namespace std;

bool resuelveCaso() {
	//n = numero de papeles
	//k = numero de actores
	//p = presupuesto total
	//m = puntuacion minima requerida
	int n, k, p, m;
	vector<int> salarioActor;
	int afinidad[50][50];			//Para cada papel la puntuacion
	cin >> n >> k >> p >> m;
	
	if (n == 0 && k == 0 && p == 0 && m == 0) return false;


}


int main() {
	while (resuelveCaso());
}